# For more information: https://laravel.com/docs/sail
version: '3'

services:
    db:
        image: postgres
        environment:
            POSTGRES_PASSWORD: "postgres"
        ports:
            - "5432:5432"
        volumes:
            - /var/lib/postgresql/data
